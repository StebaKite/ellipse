FILE DI CONFIGURAZIONE DI ELLIPSE
--------------------------------------------------------------
Il file di configurazione di Ellipse non va versionato, ciascun ambiente può utilizzare parametri di configurazione differenti

Quindi, devi :

	1. creare nella directory config un file ellipse.config.ini copiando il file ellipse.config.ini.template
	2. sistemare i parametri di configurazione in base al tuo ambiente
	3. tasto destro + Team + Ignore   per evitare il suo versionamento in GIT 